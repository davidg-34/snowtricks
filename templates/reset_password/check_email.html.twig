{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
    <section class="container my-5">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-6 border border-black p-5 text-center">
                <p>
                    Si le compte existe, un email vient d'être envoyé avec un lien pour réinitialiser votre mot de passe
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p>Si vous ne recevez pas d'email, vérifiez dans vos spams : </p><a href="{{ path('app_forgot_password_request') }}">essayez à nouveau</a>.
            </div>
        </div>
    </section>
{% endblock %}
