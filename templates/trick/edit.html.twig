{% extends 'base.html.twig' %}

{% block title %}Administration
{% endblock %}

{% block body %}

    <div class="container col-6">
        {% if editMode %}
            <h1 class="text-center">Modification de la figure</h1>
        {% else %}
            <h1 class="text-center">Création d'une figure</h1>
        {% endif %}
    </div>
    {# Formulaire pour créer/modifier une figure #}
    {% if app.user %}
        <div class="container my-5">
            <div class="container col-6">
                {{ form_start(formTrick) }}
                {{ form_errors(formTrick) }}

                {{ form_row(formTrick.name) }}
                {{ form_row(formTrick.description) }}
                {{ form_row(formTrick.category) }}
                {{ form_row(formTrick.medias) }}

                {# Boutons modifier/ajouter/supprimer une figure du formulaire #}
                <button type="submit" class="m-3">
                    {% if editMode %}
                        Modifiez
                    {% else %}
                        Ajoutez
                    {% endif %}
                </button>
                {% if editMode %}
                    <a href="/tricks/{{ trick.id }}/delete" class="button m-3" style="background-color: red; color: aliceblue;">Supprimer</a>
                {% endif %}

                {# Modification/suppression de l'image ou vidéo téléchargée #}
                {% if editMode %}
                    <div style="display: flex; gap: 15px; width: 100%;">
                        {% for media in trick.medias %}
                            {% if media.type == "picture" %}
                                <div class="edit-media">
                                    <img src="/assets/uploads/{{ media.media }}" style="width: 50px; height: 50px; border-radius: 15px;">
                                    <div class="delete-overlay">
                                        <a href="/tricks/{{ trick.id }}/medias/{{ media.id }}/delete">
                                            <span class="text-white">X</span>
                                        </a>
                                    </div>
                                </div>
                            {%  else %}
                                <!-- todo: afficher les videos -->
                                <div class="ratio ratio-16x9 muted">
                                    <iframe class="embed-responsive-item muted" src="/assets/uploads/{{ media.media }}" allowullscreen></iframe>
                                </div>
                            {%  endif %}
                        {%  endfor %}
                    </div>
                {% endif %}


                {{ form_end(formTrick) }}

            </div>
        </div>
    {% endif %}
{% endblock %}
